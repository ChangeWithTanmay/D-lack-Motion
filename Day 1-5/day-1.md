# Question
ঠিক আছে 👍
নিচে **শুধুমাত্র Dictionary Comprehension–এর প্রশ্ন** দিলাম।
সবগুলো এমনভাবে দেওয়া যাতে **dict comprehension ছাড়া practically করা যাবে না**।

---

## 🟢 Easy (1–10)

1️⃣ 1–10 পর্যন্ত সংখ্যার **square** দিয়ে dictionary বানাও

2️⃣ একটি list:

```python
nums = [2, 4, 6, 8]
```

👉 number : number×2

3️⃣ একটি list:

```python
names = ["rahim", "karim", "tamim"]
```

👉 name : length

4️⃣ একটি string:

```python
s = "python"
```

👉 character : ASCII value

5️⃣ 1–10 এর মধ্যে শুধু **odd** সংখ্যাগুলোর cube রাখো

6️⃣ একটি list:

```python
nums = [5, 10, 15]
```

👉 number : number+5

7️⃣ একটি list:

```python
words = ["apple", "banana", "cherry"]
```

👉 word : last character

8️⃣ একটি string:

```python
s = "abcd"
```

👉 character : position (a→1)

9️⃣ একটি list:

```python
nums = [3, 6, 9]
```

👉 number : number/3

🔟 1–5 পর্যন্ত
👉 number : number²

---

## 🟡 Medium (11–20)

1️⃣1️⃣ একটি list:

```python
nums = [1, 2, 3, 4, 5, 6]
```

👉 **even number** : square

1️⃣2️⃣ একটি dict:

```python
marks = {"rahim": 80, "karim": 45, "tamim": 90}
```

👉 **only passed (>=50)**

1️⃣3️⃣ একটি list:

```python
nums = [-5, -3, 0, 2, 4]
```

👉 number : absolute value

1️⃣4️⃣ একটি string:

```python
s = "banana"
```

👉 character : frequency (no duplicate count)

1️⃣5️⃣ একটি list:

```python
words = ["hi", "hello", "hey", "good"]
```

👉 word : length (length > 3)

1️⃣6️⃣ একটি list:

```python
nums = [10, 15, 20, 25]
```

👉 number : "even" / "odd"

1️⃣7️⃣ একটি dict:

```python
prices = {"apple": 120, "banana": 40, "mango": 150}
```

👉 price > 100 only

1️⃣8️⃣ একটি list:

```python
nums = [1, 2, 3, 4]
```

👉 number : "even"/"odd"

1️⃣9️⃣ একটি string:

```python
s = "abcd"
```

👉 character : character repeated twice

2️⃣0️⃣ দুটি list:

```python
keys = ["a", "b", "c"]
values = [1, 2, 3]
```

👉 key : value

---

## 🔴 Hard (21–30)

2️⃣1️⃣ একটি list:

```python
nums = [1, 2, 3, 4, 5]
```

👉 number : "prime" / "not prime"

2️⃣2️⃣ একটি sentence:

```python
s = "this is python"
```

👉 word : length

2️⃣3️⃣ একটি dict:

```python
students = {"rahim": 80, "karim": 45, "tamim": 90}
```

👉 name : "pass"/"fail"

2️⃣4️⃣ একটি list:

```python
nums = [2, 4, 6, 8]
```

👉 number : number / total_sum

2️⃣5️⃣ একটি list:

```python
words = ["Apple", "banana", "Cherry"]
```

👉 word(lowercase) : original word

2️⃣6️⃣ একটি string:

```python
s = "mississippi"
```

👉 character : frequency (use set)

2️⃣7️⃣ একটি list:

```python
nums = [1, 2, 3, 4]
```

👉 number : factorial

2️⃣8️⃣ একটি dict:

```python
data = {"a": 1, "b": 2, "c": 3}
```

👉 value : key

2️⃣9️⃣ একটি list:

```python
nums = [1, 2, 3, 4, 5]
```

👉 number : cumulative sum

3️⃣0️⃣ Nested list:

```python
matrix = [[1, 2], [3, 4]]
```

👉 index : value (flatten)

---

🔥 **Rule for you:**
✔️ প্রতিটা solution **এক লাইনের dict comprehension**
❌ normal for-loop না

যখন 5–6টা solve করবে, পাঠাও—আমি **strictly review করে দেবো** 💪
